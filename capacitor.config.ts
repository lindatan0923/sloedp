import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.electiondata.app',
  appName: 'sloedp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
